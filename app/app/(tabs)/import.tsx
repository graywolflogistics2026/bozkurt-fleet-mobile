import { PlaceholderScreen } from '@/src/components/Placeholder';

export default function Import() {
  return (
    <PlaceholderScreen
      title="Import"
      note="AI-assisted document import (settlements, receipts, fuel, W-2s) lands in a later session — the ai-import Edge Function is already deployed server-side (supabase/functions/ai-import)."
    />
  );
}
